import 'package:flutter/material.dart';

class AddPartnersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Add Partners")),
      body: Center(child: Text("Add Partners Page")),
    );
  }
}